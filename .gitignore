# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*

# Gradle 构建产物：各模组子目录（如 chatcmd/）用自已的 .gitignore 细化。
# 注意：不要在这里写通用的 *.jar，否则会误忽略每个模组子目录里的
# gradle/wrapper/gradle-wrapper.jar（构建必需），需要逐个反向排除。
/build/
.gradle/